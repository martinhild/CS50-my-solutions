#include <cs50.h>
#include <stdio.h>
#include <ctype.h>


int main(void)
{

//get Text
string text = get_string("Text: ");
//print text
printf("Text: %s\n", text);



int count_letters(string text)
{
    for (i = 0; i < text.getLength(); i++)
    {
        if (text[i])
    }
}

}