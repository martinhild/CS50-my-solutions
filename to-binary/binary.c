#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    int decimal = get_int("Integer: ");

    int binary_array[8];

    int x;
    for(int i = 0; i < 8; i++)
    {
        decimal % 2;
    }
    decimal % 2;
}